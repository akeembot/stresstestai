services:
  - type: web
    name: stresstestai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/app.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 10000
